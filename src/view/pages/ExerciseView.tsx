import * as React from 'react';
import { Course } from '../../protocol/protocol';

function ExerciseView(props: { assignment: Course, setSelectedAssignment: (assignment: Course) => void }) {
    
    
    return <>
    </>;
}

export default ExerciseView;